server {
  listen   <%= node['nginx']['port'] -%>;
  server_name  <%= node['stelligent']['site'] %>;

  access_log  <%= node['nginx']['log_dir'] %>/localhost.access.log;

  location / {
    root   /var/www/<%= node['stelligent']['site'] %>;
    index  index.html index.htm;
  }
}
